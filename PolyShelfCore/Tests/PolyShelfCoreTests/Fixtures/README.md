# Test fixtures

Small real-format files used by parser tests (M3+): tiny STL (binary + ASCII),
a minimal 3MF, a gcode file with an embedded Prusa thumbnail, plus deliberately
truncated/corrupt variants. Generated fixtures are created by the tests
themselves where possible to keep the repo small.
